# Cloned at runtime by run_bonsai.bat / run_bonsai.ko.bat - has its own .git,
# contains multi-GB model files, and isn't meant to be tracked here.
Bonsai-demo/

# Generated by the launcher on first run - user-specific saved settings.
config.bat

# Claude Code local project settings.
.claude/
